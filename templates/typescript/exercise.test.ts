import { solve } from "./exercise";

describe("exercise", () => {
  it("solves the first example from the problem description", () => {
    // Replace with the real example input/output from the LeetCode description.
    expect(solve()).toBeDefined();
  });

  it.todo("handles edge cases");
});
